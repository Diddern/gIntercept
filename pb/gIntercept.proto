syntax = "proto3";

package pb;

message request {
    uint64 a = 1;
    uint64 b = 2;
}

message response {
    uint64 result = 1;
}

service service {
    rpc Compute (request) returns (response) {}
}